>---
# MaxManager Project Structure (КРИТИЧНО)

alwaysApply: true
---

## 🏗 ЖЕСТКАЯ СТРУКТУРА - НЕ МЕНЯТЬ БЕЗ СОГЛАСОВАНИЯ!

### data/ - ЕДИНСТВЕННОЕ место для данных

```
data/
├── ini_parameters_database.json  ← ОСНОВНАЯ БАЗА (916 параметров)
├── presets/                       ← Пресеты (будущее)
└── validation/                    ← Правила валидации (будущее)
```

**ЗАПРЕЩЕНО:**
- ❌ Создавать базы в других папках (docs/, src/, etc)
- ❌ Коммитить бэкапы (*_backup*.json) в git
- ❌ Дублировать данные в разных местах

**КРИТИЧНО:**
- База ТОЛЬКО в `data/ini_parameters_database.json`
- Все загрузки ТОЛЬКО из `data/`
- Пути в коде: `Path(__file__).parent.parent.parent / 'data' / 'ini_parameters_database.json'`

### src/ - Исходный код

```
src/
├── __version__.py                 ← ЕДИНСТВЕННЫЙ источник версии!
├── data/
│   ├── database_loader.py         ← Загрузка из data/
│   └── tab_mapper.py
├── modules/
│   ├── ini_manager.py
│   ├── parameter_info_loader.py
│   └── plugin_ini_finder.py
├── ui/
│   ├── canvas_main_window.py
│   ├── collapsible_canvas.py
│   └── grid_layout_manager.py
├── maxscript/
│   └── maxmanager.mcr
└── i18n/
    └── translations.py
```

### docs/ - Документация

```
docs/
├── DEVELOPMENT.md          ← Главный документ (структура, база, workflow)
├── ROADMAP.md              ← План развития
└── VERSION_MANAGEMENT.md   ← Управление версией
```

**ЗАПРЕЩЕНО:**
- ❌ Хранить JSON данные в docs/
- ❌ Создавать дубликаты документов
- ❌ Плодить README в каждой папке

### scripts/ - Утилиты

```
scripts/
├── update_version.py       ← Автоматический бамп версии
└── test_*.py               ← Тесты
```

**ЗАПРЕЩЕНО:**
- ❌ Legacy скрипты (add_*, fix_*, improve_*)
- ❌ Временные скрипты без цели
- ❌ Дублирование логики из src/

### Корень проекта

```
MaxManager/
├── Install_MaxManager.ms   ← Установщик
├── README.md               ← Главный README (GitHub)
├── .gitignore              ← Игнорирование мусора
└── requirements.txt        ← Python зависимости
```

**ЗАПРЕЩЕНО:**
- ❌ test_*.py, test_*.ini в root
- ❌ *.log файлы
- ❌ Временные скрипты
- ❌ UI прототипы (*.png, *.html)

## 🔒 Правила при разработке

### 1. База данных
- **ЧИТАТЬ:** `data/ini_parameters_database.json`
- **ПИСАТЬ:** `data/ini_parameters_database.json`
- **НЕ создавать:** копии, бэкапы, дубликаты

### 2. Версия
- **ЧИТАТЬ:** `src/__version__.py`
- **МЕНЯТЬ:** `python scripts/update_version.py X.Y.Z`
- **НЕ хардкодить:** версию в файлах

### 3. Документация
- **ОСНОВНОЙ:** `docs/DEVELOPMENT.md`
- **ОБНОВЛЯТЬ:** при изменении структуры
- **НЕ плодить:** README в каждой папке

### 4. Скрипты
- **УТИЛИТЫ:** `scripts/update_version.py`
- **ТЕСТЫ:** `scripts/test_*.py`
- **УДАЛЯТЬ:** после завершения одноразовые

### 5. Очистка
- **git add -A** перед коммитом
- **НЕ коммитить:** logs, tests, backups
- **.gitignore** защитит от мусора

## ⚠️ КРИТИЧНЫЕ ЗАПРЕТЫ

**НЕ ДЕЛАТЬ БЕЗ СОГЛАСОВАНИЯ:**
1. Переименовывать `data/` папку
2. Перемещать базу из `data/`
3. Создавать копии базы в других местах
4. Менять версию не через `__version__.py`
5. Плодить документы в docs/
6. Оставлять legacy скрипты

**ПРИ НАРУШЕНИИ:**
- Немедленно откатить через git
- Восстановить правильную структуру
- Обновить этот файл правил

## 📋 Чеклист перед коммитом

- [ ] База ТОЛЬКО в `data/ini_parameters_database.json`
- [ ] Нет бэкапов/копий базы в git
- [ ] Нет test_*.py, *.log в root
- [ ] Нет дублирующих документов
- [ ] Версия ТОЛЬКО в `src/__version__.py`
- [ ] .gitignore актуален

## 🔄 При изменении структуры

1. **Обсудить** с пользователем
2. **Обновить** этот файл правил
3. **Обновить** `docs/DEVELOPMENT.md`
4. **Обновить** `.gitignore`
5. **Закоммитить** с префиксом `chore: project structure`

---

**Цель:** Предотвратить бардак, как было раньше (60+ мусорных файлов удалено)
